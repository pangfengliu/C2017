#include <stdio.h>

int main()
{
  int n;
  scanf("%d", &n);
  int numbers[2048];
  for (int i = 0; i < n i++)
    scanf("%d", &numbers[i]);

  int support = balance(numbers, n);
  printSeesaw(numbers, n);
}
